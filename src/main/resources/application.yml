spring:
  application:
    name: test-proj
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
server:
  port: 8080
springdoc:
  swagger-ui:
    path: /swagger-ui.html
